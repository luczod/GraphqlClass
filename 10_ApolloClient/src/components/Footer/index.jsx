import P from 'prop-types';
import * as Styled from './styles';

export const Footer = () => {
  return (
    <Styled.Container>
      <div></div>
    </Styled.Container>
  );
};

Footer.propTypes = {
  children: P.node,
};
